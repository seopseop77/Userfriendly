# Current Status (resume entry point)

> **Updated by the active Claude Code session at every checkpoint.**
> A new session reads this file → the active worklog → `git log -5`, then
> executes "Next single step". See `/CLAUDE.md §5, §6` for the rules.

---

**Last updated**: 2026-05-06 (Phase 1b checkpoint 8 complete)
**Updated by**: Claude Code

## Current phase

- **Phase**: Phase 1b — security boundary hardening (cleanup pass in progress)
- **Active task**: Manifest signature verification live; `on_persisted` ordering fix is next (Checkpoint C).

## Active worklog

`docs/worklog/2026-05-05-phase1b-security.md`

## Recent commits

```
3010aae   security: wire manifest signature verifier into loader
592ccc4   docs: Phase 1b checkpoint 7 — EgressGuard wired into proxy lifespan
e2ee4f0   proxy: wire EgressGuard into lifespan
d089fc0   docs: Phase 1b checkpoint 6 — signature verifier primitive
2659284   security: ed25519 manifest signature verifier
```

## Where we paused

Phase 1b cleanup-pass checkpoint B complete (2026-05-06, commit
3010aae). Manifest signature verification now runs at plugin load
time end-to-end:

- New `trust/` package with bundled `keys.toml`. First entry:
  `minseop` (public key generated by user via the new CLI; private
  half held in OS keychain).
- `PluginHost._verify_manifest(plugin_class)` reads `plugin.toml`
  byte-exact + sibling `plugin.toml.sig`, calls
  `verify_manifest_signature` against the bundled registry.
  `load_plugins()` runs it between manifest validation and
  capability denial; non-VERIFIED outcomes write
  `manifest_rejected` (kind reused per ADR-0008) with the reason
  + signer in `detail_json`.
- New CLI subcommands: `llm-tracker generate-key <name>` (ed25519
  keypair, private in keychain) and `llm-tracker sign-plugin
  <pkg-path> --signer <name>` (writes sibling `.sig` TOML blob).
- `hello_world` reference plugin signed by `minseop`; `.sig`
  checked in.
- Three new tests: integration (real entry-point + real registry
  + real `.sig`), and two loader-level reject paths
  (SIGNATURE_MISSING, SIGNING_KEY_NOT_IN_REGISTRY-with-signer).
  `_bypass_verifier(monkeypatch)` helper added so prior
  load-plugins tests using fake EPs continue to pass.

107/107 tests pass; cleanup-pass scope ruff clean (one
pre-existing I001 in `cli/main.py:17` unchanged from prior
Suggestions list).

Cleanup pass progress: A and B closed (proxy-boot wiring,
manifest signing). Remaining: C (on_persisted ordering), D
(synthetic SSE block response), E (audit_log triggers), F
(ADR-0008 housekeeping), G (session_factory property + ADR-0009
for `allowed_modes` default). Then Gates 1/2 with user input.

## Next single step

**Checkpoint C — fix `on_persisted` ordering in the forwarder.**
`packages/llm_tracker/src/llm_tracker/proxy/forwarder.py:109–125`
currently runs `on_response_complete → on_persisted →
record_exchange_timing`. Per design.md §6.3.2, `on_persisted`
runs *after* the DB write. Move `record_exchange_timing(...)`
ahead of `plugin_host.on_persisted(...)`.

Add a regression test: a fake plugin whose `on_persisted` opens
a session and reads the `exchanges` row; assert the row is
non-empty (i.e., the timing write happened first).

## Blocking / decisions needed

- None for Checkpoint C — surgical reorder + one regression test.
- Gates 1 (Transform handling) and 2 (hook payload routing)
  remain deferred to their respective checkpoints.

## Progress

- [x] Design v0.1 written
- [x] Framework pivot v0.2
- [x] English-only documentation pass
- [x] ADRs 0001–0008 sealed (0004 superseded by 0007)
- [x] Phase 0 — core skeleton (CLOSED 2026-05-04)
- [x] Phase 1a — plugin SDK (CLOSED 2026-05-05)
- [ ] Phase 1b — security boundary hardening (in progress)
- [ ] Phase 1c — `scope_guard` plugin
- [ ] Phase 2+ — Mode R sink, third-party plugins

---

## Update rules (for Claude Code)

At every checkpoint, do these three as one atomic unit (CLAUDE.md §5.3):

1. `git commit` the code change (CLAUDE.md §11).
2. Append the new commit hash to the active worklog's "What was done"
   section, and rewrite the "What's left / Handoff" section as of *now*.
3. Refresh this STATUS.md:
   - Last-updated timestamp (YYYY-MM-DD).
   - Active worklog path.
   - Last 3–5 commits.
   - "Where we paused".
   - "Next single step".

If you don't bundle these three, the next session won't know where to pick
up.
