# 2026-05-28 r003 · Bash tool_result shape

**Hypothesis**: Bash `tool_result` blocks classify cleanly as
`tool_result`, increment `turn_seq` like other tools, and carry stdout
in the same content shape — confirm whether Bash adds any unique fields
to the block.

**Conversation**: `01KSQ9FJ6WCM8WSVP7Q79Q6SVB`
**UUID**: `19a87eb7-2b57-4a45-bc83-d486d3918251`

**Prompts**:
- t01: `[PROBE 2026-05-28 r003 t01] Use the Bash tool to run exactly:
  echo hello world. Report what Bash returned, verbatim.`
- t02: `[PROBE 2026-05-28 r003 t02] Use Bash to run: wc -l hello.py
  calculator.py today.py. Report the line counts.` *(pending full query
  after t02 fires — will be appended once row 3/4 land)*

## Findings

1. **Bash `tool_result` block has an extra `is_error: false` field**.
   ```
   [{"type":"tool_result", "content":"hello world",
     "is_error": false, "tool_use_id": "toolu_..."}]
   ```
   Read/Glob/Grep `tool_result` blocks (r001, r002) did **not** include
   `is_error`. So this field's presence/absence varies by tool — Bash
   always emits it explicitly, the others omit it. Classifier still
   routes both to `role = 'tool_result'` correctly because the rule is
   "every block is `type=tool_result`", not "every block matches a
   fixed schema".
   - **Implication for r010**: Bash on a *failed* command should set
     `is_error: true`; that's the value the role classifier needs to
     stay invariant to. Will verify directly there.
2. **Row count + roles + cache**: clean 1:1 with r001/r002 patterns. No
   anomalies.

## Score
- ✅ row count, conversation_id, turn_seq, role, cache chain
- ✅ no segfault on `new` (3rd success in 4 attempts; r001 t01 the lone
  outlier)
