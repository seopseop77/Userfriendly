# 2026-05-28 r007 · WebSearch

**UUID**: `eea91e98-…` · 1 turn, 2 rows. (Skipped t02 — t01 was
sufficient to answer the hypothesis.)

## Observed

| seq | role | stop | content shape |
|---|---|---|---|
| 1 | user_input  | tool_use | t01 prompt with `cache_control` |
| 2 | tool_result | end_turn | `[{type:"tool_result", content:"<long body>", cache_control:{ttl:"1h",type:"ephemeral"}}]` |

## Findings

1. **WebSearch `tool_result` envelope** starts with `Web search results
   for query: "…"\n\nLinks: [...]` and ends with the framework-injected
   instruction `REMINDER: You MUST include the sources above in your
   response to the user using markdown hyperlinks.` — a prompt-injection
   the framework appends to every WebSearch result to enforce citation
   style.
2. **`cache_control` on WebSearch `tool_result` block too** — same
   pattern as WebFetch (r006). Confirms framework-typed `tool_result`
   blocks routinely carry `cache_control`. ADR-0038 prose about the
   cache_control distribution is now contradicted by two distinct tools
   (WebFetch, WebSearch).
3. **The registered framework auto-call prefix `"Perform a web search
   for the query: "` did not appear in this round.** Like WebFetch's
   `"Web page content:\n---\n"` envelope, that prefix is either
   - emitted on a different code path (likely Claude Code's `/web*`
     slash command or an internal auto-search), not the `WebSearch`
     tool-use path, OR
   - currently dead.
   - **Should be checked against historic data** in the consolidated
     report — see also r006's note.
4. **No segfault, clean row alternation, turn_seq 1→2.**

## Hypotheses retired by this round
- "WebSearch tool-use produces the registered framework auto-call
  wrapper": **rejected** for the model-driven WebSearch path. The
  prefix is reserved for some other path. Same outcome as r006 for
  WebFetch's prefix.
