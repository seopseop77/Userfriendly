# 2026-05-28 r008 + r009 · covered by earlier rounds

**r008 hypothesis (parallel tool_use → single tool_result row with
N blocks)** was directly verified by **r002** (2-way and 3-way parallel
both collapsed to a single sink row with the expected `n_blocks` count;
turn_seq incremented by exactly 1). No need for a fresh round.

**r009 hypothesis (model-initiated tool chain depth ≥ 3 within one
user message preserves gap-free turn_seq)** was directly verified by
**r004 t01** — the sub-session ran Read → Edit → Bash on its own from
one prompt, producing 4 contiguous rows (user_input + 3 × tool_result)
with `turn_seq` 1→2→3→4 and `stop_reason` flipping from `tool_use` on
the chained rows to `end_turn` on the terminator.

Both retired. Campaign moves on to r010 (tool failure / is_error=True).
